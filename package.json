{
  "name": "stack-mcp",
  "version": "1.0.0",
  "description": "MCP Server para gerenciar stack: n8n, Evolution API, Chatwoot, MongoDB, Redis, Qdrant, Coolify",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run build:dashboard",
    "build:backend": "tsc",
    "build:dashboard": "cd dashboard && npm install && npm run build",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "ioredis": "^5.3.2",
    "mongodb": "^6.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "bin": {
    "stack-mcp": "./dist/index.js"
  }
}
